// circle area, perimeter

void main(){
  int radius = 5;
  const pi = 3.14;

  print("Area and Perimeter of cicle with radius $radius are ${radius * radius * pi} and ${2 * radius * pi} respectively.");

  // pi = 2.7;

  double area = radius * radius * pi;
  double perimeter = 2 * radius * pi;

  print("Area and Perimeter of cicle with radius $radius are $area and $perimeter respectively.");
}